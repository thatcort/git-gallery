<!DOCTYPE html>
<html>
	<script>
		var readyScripts = []; // scripts to be run once the document is ready
	</script>

  {{{body}}}

	<script>
		$(document).ready(function() {
			readyScripts.forEach(script => { try { script(); } catch (e) { console.log(e); }});
		});
	</script>
</html>
